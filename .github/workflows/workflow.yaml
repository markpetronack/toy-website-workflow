name: deploy-toy-website

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'build step'
        run: echo "The Build Step"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'dev'
    steps:
      - name: 'dev deploy'
        run: echo '${{ vars.myenv }}'

  